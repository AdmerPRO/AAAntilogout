# Contributing

## Development

- Use Java 21.
- Build with Maven from the `AAAntilogout` folder.
- Keep code comments in English.
- Keep player-facing Polish messages configurable in `config.yml`.
- Run `mvn clean package` before publishing changes.

## Pull Requests

Describe what changed, how it was tested and whether config migration is needed.
